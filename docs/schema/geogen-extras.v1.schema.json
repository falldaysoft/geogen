{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://geogen.dev/schema/geogen-extras.v1.schema.json",
  "title": "geogen glTF node extras, version 1",
  "description": "Gameplay metadata geogen writes to glTF node extras under the 'geogen' key. Unknown keys are allowed so newer exporters stay readable; runtimes check 'version'.",
  "type": "object",
  "required": ["geogen"],
  "properties": {
    "geogen": {
      "type": "object",
      "required": ["version"],
      "properties": {
        "version": { "const": 1 },
        "tags": {
          "description": "Semantic tags, dotted from general to specific (room, room.hotel_bedroom, furniture.bed, door.interior).",
          "type": "array",
          "items": { "type": "string", "pattern": "^[a-z0-9_]+(\\.[a-z0-9_]+)*$" }
        },
        "type": {
          "description": "Special node kinds. room_volume: axis-aligned box (in the node's frame) of size 'size' centred on the node. spawn: player start, facing the node's +Z. collider: collision-only geometry (Godot suffix -colonly = trimesh, -convcolonly = convex); do not render. material_group: helper node whose primitives were folded into its parent's mesh (left unreferenced). npc: a character placed here (see 'npc'); its body asset is the child named by npc.body.node.",
          "enum": ["room_volume", "spawn", "collider", "light", "lod", "material_group", "npc"]
        },
        "shape": {
          "description": "Collider nodes only: how the collision geometry was made.",
          "enum": ["box", "hull", "mesh"]
        },
        "collider": {
          "description": "Mesh nodes: the collider chosen for this mesh (none = no collision). The collider itself is a child node.",
          "enum": ["none", "box", "hull", "mesh"]
        },
        "walkable": {
          "description": "Surface meant to be walked on (navmesh input).",
          "type": "boolean"
        },
        "room": {
          "description": "Room identity, on room nodes and their room_volume.",
          "type": "object",
          "required": ["id", "type"],
          "properties": {
            "id": { "type": "string" },
            "type": { "type": "string" }
          }
        },
        "door": {
          "description": "Door/opening nodes generated by floor plans.",
          "type": "object",
          "properties": {
            "style": { "enum": ["door", "archway"] },
            "hinge": { "enum": ["left", "right"] },
            "width": { "type": "number" },
            "height": { "type": "number" },
            "rooms": { "type": "array", "items": { "type": "string" } },
            "swing_room": { "type": ["string", "null"] }
          }
        },
        "joint": {
          "description": "Articulation pivot (e.g. a door leaf's hinge): children rotate about 'axis' (node frame) between min_deg and max_deg.",
          "type": "object",
          "required": ["type", "axis"],
          "properties": {
            "type": { "enum": ["hinge"] },
            "axis": { "type": "array", "items": { "type": "number" }, "minItems": 3, "maxItems": 3 },
            "min_deg": { "type": "number" },
            "max_deg": { "type": "number" }
          }
        },
        "interactions": {
          "description": "Asset nodes: state machines that move descendant nodes (by exported node name). Exported transforms show the 'initial' state; a part's rest pose is M(initial value)^-1 * its exported transform, and its pose for value v is asset * M(v) * rest, where M rotates by v degrees about 'axis' through 'pivot' (asset frame) or translates v metres along 'axis'. use() goes to states[target].next; 'then' auto-advances on arrival; 'emit' names an event raised on arrival.",
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "required": ["targets", "initial", "duration", "states", "motions"],
            "properties": {
              "targets": { "type": "array", "items": { "type": "string" } },
              "initial": { "type": "string" },
              "duration": { "type": "number", "minimum": 0 },
              "lock": {
                "description": "While locked, use() fails; holders of 'key' can lock/unlock.",
                "type": "object", "required": ["key", "locked"],
                "properties": { "key": { "type": "string" }, "locked": { "type": "boolean" } }
              },
              "states": {
                "type": "object",
                "additionalProperties": {
                  "type": "object",
                  "properties": {
                    "next": { "type": "string" },
                    "then": { "type": "string" },
                    "emit": { "type": "string" },
                    "prompt": { "type": "string" },
                    "after": { "type": "number", "minimum": 0, "description": "Dwell before 'then' (auto-close)." }
                  },
                  "additionalProperties": false
                }
              },
              "motions": {
                "type": "array",
                "items": {
                  "type": "object",
                  "required": ["nodes", "type", "axis", "pivot", "values"],
                  "properties": {
                    "nodes": { "type": "array", "items": { "type": "string" } },
                    "type": { "enum": ["rotate", "translate"] },
                    "axis": { "type": "array", "items": { "type": "number" }, "minItems": 3, "maxItems": 3 },
                    "pivot": { "type": "array", "items": { "type": "number" }, "minItems": 3, "maxItems": 3 },
                    "values": { "type": "object", "additionalProperties": { "type": "number" } }
                  }
                }
              }
            }
          }
        },
        "footprint": {
          "description": "Furniture: floor footprint [x, z] in metres (asset frame, centred on the origin).",
          "type": "array", "items": { "type": "number", "minimum": 0 }, "minItems": 2, "maxItems": 2
        },
        "clearance": {
          "description": "Furniture: free space needed beyond the footprint on each side (front = +Z), metres.",
          "type": "object",
          "properties": {
            "front": { "type": "number" }, "back": { "type": "number" },
            "left": { "type": "number" }, "right": { "type": "number" }
          },
          "additionalProperties": false
        },
        "door_swings": {
          "description": "Room nodes: door leaf swing arcs in the room's frame (hinge point, radius, sweep in degrees in the XZ plane, 0 = +X, 90 = +Z), for furnishing to keep clear.",
          "type": "array",
          "items": {
            "type": "object",
            "required": ["door", "hinge", "radius", "from_deg", "to_deg"]
          }
        },
        "light": {
          "description": "Light fixtures: an omni light at 'offset' (node frame). Exported as a KHR_lights_punctual point light on a <node>_light child (intensity = energy * 12 cd).",
          "type": "object",
          "properties": {
            "type": { "const": "omni" },
            "color": { "type": "array", "items": { "type": "number" }, "minItems": 3, "maxItems": 3 },
            "energy": { "type": "number" }, "range": { "type": "number" },
            "offset": { "type": "array", "items": { "type": "number" }, "minItems": 3, "maxItems": 3 }
          }
        },
        "switch": {
          "description": "Light switches: the light node (by name, in the same model) this switch's interaction turns on/off.",
          "type": "object", "required": ["light"], "properties": { "light": { "type": "string" } }
        },
        "affordances": {
          "description": "Things an actor can do at an asset (asset frame): sit/lie/use/stand/look at 'position' facing 'yaw' degrees about +Y (0 = +Z); 'height' is where the hips rest. The player takes sit/lie; NPCs run 'action' (assets/npcs/actions.yaml), starting from 'approach' (floor level), for 'duration' seconds, adding 'advertises' to their needs; 'slots' actors at once; 'interaction' is the asset interaction a use step drives.",
          "type": "array",
          "items": {
            "type": "object", "required": ["type", "position", "yaw"],
            "properties": {
              "type": { "enum": ["sit", "lie", "use", "stand", "look"] },
              "position": { "$ref": "#/$defs/vec3" },
              "yaw": { "type": "number" }, "height": { "type": "number" }, "prompt": { "type": "string" },
              "action": { "type": "string" },
              "approach": { "$ref": "#/$defs/vec3" },
              "duration": { "$ref": "#/$defs/range" },
              "advertises": { "type": "object", "additionalProperties": { "type": "number" } },
              "tags": { "type": "array", "items": { "type": "string" } },
              "slots": { "type": "integer", "minimum": 1 },
              "interaction": { "type": "string" }
            }
          }
        },
        "portal": {
          "description": "A doorway NPC paths may cross (asset frame): width x height opening centred on 'center', 'depth' thick along 'normal'. A path through it while 'interaction' isn't in state 'open' runs the NPC 'pass' action (stop 'clearance' beyond the near face, open, walk through, maybe close).",
          "type": "object",
          "required": ["interaction", "open", "closed", "center", "normal", "width", "height", "depth", "clearance"],
          "properties": {
            "interaction": { "type": "string" }, "open": { "type": "string" }, "closed": { "type": "string" },
            "center": { "$ref": "#/$defs/vec3" }, "normal": { "$ref": "#/$defs/vec3" },
            "width": { "type": "number" }, "height": { "type": "number" }, "depth": { "type": "number" },
            "clearance": { "type": "number" }
          }
        },
        "poses": {
          "description": "Character body assets: named poses as root transforms relative to an anchor (offset m, rotation deg XYZ, scale).",
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/pose" }
        },
        "npc": {
          "description": "A placed NPC, fully resolved from assets/npcs/<definition>.yaml: everything the runtime interpreter needs. Distances in metres, rates per second. 'home.polygon' is [x, z] in this node's frame.",
          "type": "object",
          "required": ["definition", "body", "speed", "needs", "activities", "scoring", "actions", "home", "radius", "height"],
          "properties": {
            "definition": { "type": "string" },
            "body": {
              "type": "object", "required": ["node", "poses"],
              "properties": { "asset": { "type": "string" }, "node": { "type": "string" },
                "poses": { "type": "object", "additionalProperties": { "$ref": "#/$defs/pose" } } }
            },
            "speed": { "type": "number" }, "turn_speed": { "type": "number" }, "seed": { "type": "integer" },
            "radius": { "type": "number" }, "height": { "type": "number" },
            "needs": { "type": "object", "additionalProperties": {
              "type": "object", "required": ["initial", "decay"],
              "properties": { "initial": { "type": "number", "minimum": 0, "maximum": 1 }, "decay": { "type": "number" } } } },
            "preferences": { "type": "object", "additionalProperties": { "type": "number" } },
            "activities": { "type": "object", "additionalProperties": {
              "type": "object", "required": ["action", "advertises"],
              "properties": { "action": { "type": "string" }, "duration": { "$ref": "#/$defs/range" },
                "advertises": { "type": "object", "additionalProperties": { "type": "number" } } } } },
            "scoring": { "type": "object", "properties": {
              "distance": { "type": "number" }, "recency": { "type": "number" }, "memory": { "type": "number" },
              "noise": { "type": "number" }, "retry": { "type": "number" } } },
            "home_margin": { "type": "number" },
            "flags": { "type": "object", "additionalProperties": { "type": "boolean" } },
            "actions": { "type": "object", "additionalProperties": {
              "type": "object", "required": ["steps"],
              "properties": { "approach": { "$ref": "#/$defs/vec3" }, "duration": { "$ref": "#/$defs/range" },
                "steps": { "type": "array", "items": { "type": "object" } } } } },
            "home": {
              "type": "object", "required": ["polygon", "y"],
              "properties": { "polygon": { "type": "array", "items": { "type": "array", "items": { "type": "number" }, "minItems": 2, "maxItems": 2 } },
                "y": { "type": "number" } }
            }
          }
        },
        "container": {
          "description": "Storage: how many items it holds, and which part is the storage volume.",
          "type": "object", "required": ["capacity"],
          "properties": { "capacity": { "type": "integer", "minimum": 0 }, "part": { "type": "string" } }
        },
        "gate": {
          "description": "A barrier that is solid (collider on, visible) except while the named interaction on an ancestor node has arrived at state open_in (e.g. a lift shaft door that opens when the car is at that floor).",
          "type": "object",
          "required": ["interaction", "open_in"],
          "properties": { "interaction": { "type": "string" }, "open_in": { "type": "string" } }
        },
        "nav": {
          "description": "false on rooms/room volumes that aren't meant to be walked (lift shafts); playtests skip them.",
          "type": "boolean"
        },
        "size": {
          "description": "room_volume only: box size in metres [x, y, z].",
          "type": "array",
          "items": { "type": "number", "minimum": 0 },
          "minItems": 3,
          "maxItems": 3
        },
        "city": { "description": "City district root: {blocks: [nx, nz], extent: [x, z]}.", "type": "object" },
        "block": { "description": "City block: {index: [i, j]}.", "type": "object" },
        "lot": { "description": "City lot: {block, zone, side, rect: [x0, z0, x1, z1], frontage, depth}.", "type": "object" },
        "building": { "description": "Building root {storeys, height}, or a placed city building {entry, zone, params?}.", "type": "object" },
        "street_furniture": { "description": "Kind of street furniture: lamp, tree, bench, hydrant, ...", "type": "string" },
        "recipe": { "description": "Building recipe name and the params it was built with.", "type": "object" },
        "roof": { "description": "Pitched roof: {style, rise}.", "type": "object" },
        "lod": { "description": "Exterior LOD stand-in of a chunk: {ratio, min_size}.", "type": "object" },
        "interior": { "description": "Root of a streamed building interior chunk: {building}.", "type": "object" },
        "scatter": { "description": "Scattered copy: {group, index, seed}.", "type": "object" }
      },
      "allOf": [
        { "if": { "properties": { "type": { "const": "room_volume" } }, "required": ["type"] },
          "then": { "required": ["room", "size"] } },
        { "if": { "properties": { "type": { "const": "collider" } }, "required": ["type"] },
          "then": { "required": ["shape"] } }
      ]
    }
  },
  "$defs": {
    "vec3": { "type": "array", "items": { "type": "number" }, "minItems": 3, "maxItems": 3 },
    "range": { "type": "array", "items": { "type": "number" }, "minItems": 2, "maxItems": 2 },
    "pose": {
      "type": "object", "required": ["offset", "rotation", "scale"],
      "properties": { "offset": { "$ref": "#/$defs/vec3" }, "rotation": { "$ref": "#/$defs/vec3" }, "scale": { "$ref": "#/$defs/vec3" } }
    }
  }
}