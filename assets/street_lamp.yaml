# Classic street lamp post
# Tall post with decorative lantern on top
# Origin is at bottom_center
# Using size [1, 1, 1] so all dimensions are in meters

name: street_lamp
origin: bottom_center
size: [1, 1, 1]  # 1m cube so sizes are direct meters

parts:
  # Base plate (0.3m diameter, 0.02m thick)
  base_plate:
    primitive: cylinder
    size: [0.30, 0.30, 0.02]
    anchor: bottom_center
    material: metal

  # Decorative base lower
  base_lower:
    primitive: cylinder
    size: [0.25, 0.25, 0.05]
    attach_to: base_plate
    at: top
    from: bottom
    material: metal

  # Decorative base middle
  base_middle:
    primitive: cylinder
    size: [0.18, 0.18, 0.06]
    attach_to: base_lower
    at: top
    from: bottom
    material: metal

  # Main post (tall and thin)
  post:
    primitive: cylinder
    size: [0.08, 0.08, 2.0]
    attach_to: base_middle
    at: top
    from: bottom
    material: metal

  # Upper collar
  collar:
    primitive: cylinder
    size: [0.12, 0.12, 0.04]
    attach_to: post
    at: top
    from: bottom
    material: metal

  # Lamp bracket base
  bracket:
    primitive: cylinder
    size: [0.15, 0.15, 0.03]
    attach_to: collar
    at: top
    from: bottom
    material: metal

  # Lantern body (cylindrical glass housing)
  lantern_body:
    primitive: cylinder
    size: [0.22, 0.22, 0.25]
    attach_to: bracket
    at: top
    from: bottom
    material: metal

  # Lantern top ring
  lantern_ring:
    primitive: cylinder
    size: [0.24, 0.24, 0.02]
    attach_to: lantern_body
    at: top
    from: bottom
    material: metal

  # Lantern roof
  lantern_roof:
    primitive: cone
    size: [0.30, 0.12, 0.30]
    attach_to: lantern_ring
    at: top
    from: bottom
    material: metal

  # Top finial
  finial:
    primitive: sphere
    size: [0.06, 0.06, 0.06]
    attach_to: lantern_roof
    at: top
    from: bottom
    material: metal
